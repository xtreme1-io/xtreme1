debug: ${X1_DEBUG:false}

server:
  address: 0.0.0.0
  port: 8080
  shutdown: graceful

spring:
  application:
    name: x1-community
  datasource:
    url: ${X1_SPRING_DATASOURCE_URL:jdbc:mysql://mysql:3306/x1_community}
    username: ${X1_SPRING_DATASOURCE_USERNAME:x1_community}
    password: ${X1_SPRING_DATASOURCE_PASSWORD:Rc4K3L6f}
    hikari:
      connection-test-query: SELECT 1
      connection-timeout: 60000
      max-lifetime: 54000
      idle-timeout: 50000
  redis:
    host: ${X1_SPRING_REDIS_HOST:redis}
    port: ${X1_SPRING_REDIS_PORT:6379}
    password: ${X1_SPRING_REDIS_PASSWORD:}

jwt:
  secret: v662+!pb&7#N4m0Vdc9@9tJ8@^3p&DVQ
  issuer: BasicAI
  expireHours: 120

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:/mybatis/mapper/*.xml
  type-aliases-package: ai.basic.x1.adapter.port.dao.mybatis.model

minio:
  endpoint: ${X1_MINIO_ENDPOINT:http://minio/}
  accessKey: ${X1_MINIO_ACCESSKEY:admin}
  secretKey: ${X1_MINIO_SECRETKEY:1tQB970y}
  bucketName: ${X1_MINIO_BUCKETNAME:x1-community}
